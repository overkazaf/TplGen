<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" ><mapper namespace="main.java.com.reponame.projname.mapper.UserMapper">	<sql id="Base_Column_List">	id,phone,age,name,gender,password	</sql>		<select id="queryUserById" parameterType="int" resultType="user">		SELECT			<include refid="Base_Column_List" />		FROM		User		WHERE		id = #{id}	</select>		<select id="queryAllUsers" parameterType="int" resultType="user">		SELECT			<include refid="Base_Column_List" />		FROM		User	</select>		<insert id="addUser" parameterType="user" useGeneratedKeys="true" keyProperty="id">	INSERT	INTO	User	(		<include refid="Base_Column_List" />	)	VALUES	(		#{id,jdbcType=Integer},#{phone,jdbcType=String},#{age,jdbcType=Integer},#{name,jdbcType=String},#{gender,jdbcType=byte[]},#{password,jdbcType=String}	)	</insert>		<insert id="addAllUsers" parameterType="java.util.List">  	    INSERT 	    INTO 	    User	    (  			<include refid="Base_Column_List" />  		) 		VALUES 		<foreach collection="entityList" item="item" index="index" open="(" separator="," close=")" >  	    	#{id,jdbcType=Integer},#{phone,jdbcType=String},#{age,jdbcType=Integer},#{name,jdbcType=String},#{gender,jdbcType=byte[]},#{password,jdbcType=String}		</foreach>	</insert>  		<delete id="removeUserById" parameterType="int">		DELETE		FROM		User		WHERE		id = #{id}	</delete>		<delete id="removeAllUsers" parameterType="java.util.List">		DELETE		FROM		User		WHERE		id		IN		<foreach item="item" index="index" collection="idList" open="(" separator="," close=")">			#{item.id}		</foreach>	</delete>			<update id="modifyUser" parameterType="user">		UPDATE		User		SET		phone=#{phone,jdbcType=VARCHAR},age=#{age,jdbcType=INTEGER},name=#{name,jdbcType=VARCHAR},gender=#{gender,jdbcType=BLOB},password=#{password,jdbcType=VARCHAR}		WHERE		id = #{id}	</update>		<update id="modifyAllUsers" parameterType="java.util.List">		UPDATE		User		SET		phone=#{phone,jdbcType=VARCHAR},age=#{age,jdbcType=INTEGER},name=#{name,jdbcType=VARCHAR},gender=#{gender,jdbcType=BLOB},password=#{password,jdbcType=VARCHAR}		WHERE		id 		IN		<foreach item="item" index="index" collection="idList" open="(" separator="," close=")">			#{item.id}		</foreach>	</update></mapper>