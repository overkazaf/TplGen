<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" ><mapper namespace="main.java.com.reponame.projname.mapper.RoleMapper">	<sql id="Base_Column_List">	id,modified_by,role_name,previlige_level,create_by	</sql>		<select id="queryRoleById" parameterType="int" resultType="role">		SELECT			<include refid="Base_Column_List" />		FROM		Role		WHERE		id = #{id}	</select>		<select id="queryAllRoles" parameterType="int" resultType="role">		SELECT			<include refid="Base_Column_List" />		FROM		Role	</select>		<insert id="addRole" parameterType="role" useGeneratedKeys="true" keyProperty="id">	INSERT	INTO	Role	(		<include refid="Base_Column_List" />	)	VALUES	(		#{id,jdbcType=Integer},#{modified_by,jdbcType=String},#{role_name,jdbcType=String},#{previlige_level,jdbcType=Integer},#{create_by,jdbcType=String}	)	</insert>		<insert id="addAllRoles" parameterType="java.util.List">  	    INSERT 	    INTO 	    Role	    (  			<include refid="Base_Column_List" />  		) 		VALUES 		<foreach collection="entityList" item="item" index="index" open="(" separator="," close=")" >  	    	#{id,jdbcType=Integer},#{modified_by,jdbcType=String},#{role_name,jdbcType=String},#{previlige_level,jdbcType=Integer},#{create_by,jdbcType=String}		</foreach>	</insert>  		<delete id="removeRoleById" parameterType="int">		DELETE		FROM		Role		WHERE		id = #{id}	</delete>		<delete id="removeAllRoles" parameterType="java.util.List">		DELETE		FROM		Role		WHERE		id		IN		<foreach item="item" index="index" collection="idList" open="(" separator="," close=")">			#{item.id}		</foreach>	</delete>			<update id="modifyRole" parameterType="role">		UPDATE		Role		SET		modified_by=#{modifiedBy,jdbcType=VARCHAR},role_name=#{roleName,jdbcType=VARCHAR},previlige_level=#{previligeLevel,jdbcType=INTEGER},create_by=#{createBy,jdbcType=VARCHAR}		WHERE		id = #{id}	</update>		<update id="modifyAllRoles" parameterType="java.util.List">		UPDATE		Role		SET		modified_by=#{modifiedBy,jdbcType=VARCHAR},role_name=#{roleName,jdbcType=VARCHAR},previlige_level=#{previligeLevel,jdbcType=INTEGER},create_by=#{createBy,jdbcType=VARCHAR}		WHERE		id 		IN		<foreach item="item" index="index" collection="idList" open="(" separator="," close=")">			#{item.id}		</foreach>	</update></mapper>